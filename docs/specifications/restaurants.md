# Restaurants

## Core entity
Restaurant records migrate from ListingPro listings and keep `legacy_wp_id`.
Suggested core data includes name/slug/description/status/owner, address/postcode/city/geolocation, contact info, pricing, halal/certification information, social/website URLs (private from crawlable public markup), media, hours, categories/features, verification/claim status and SEO fields.

`contact_email` is not an independently editable restaurant field in the account space: it mirrors the sole reference address of the user who currently represents the restaurant. Historical claim/submission e-mail snapshots are not public contact data and are retained only for audit continuity.

## Geography
`city_code` est l'identifiant unique de la commune ; `city_name` est son libellé public. Les pages ville, département et région utilisent un référentiel administratif local versionné qui résout ce code, sans appel distant et sans dupliquer département ou région dans chaque restaurant. Les cas Paris/Lyon/Marseille, 2A/2B et DOM/COM sont traités dans ce référentiel. Les coordonnées restent disponibles pour les recherches spatiales/distance de MariaDB.

## Structured address contract

- `address` is retained unchanged as the historical/raw address for legacy records.
- `address_line1` contains only the number and street; `postal_code` and `city_name` are separate fields.
- `AddressLineParser` is the single deterministic implementation for this split. It removes a suffix only when the raw/structured postcode and city agree strictly and a non-empty street remains.
- A confirmed cleanup mode may remove an explicit final historical `CP + ville` from `address_line1` even when it conflicts with structured data; it never changes `address`, `postal_code` or `city_name`, and requires a non-empty street before the suffix.
- New or replacement addresses require a Géoplateforme selection resolved server-side. A usable selection supplies `address_line1`, postcode, city, INSEE code, country and valid GPS coordinates; no manual administrative/GPS entry is accepted.
- The persisted localisation contract is `address`, `address_line1`, `address_line2`, `postal_code`, `city_name`, `city_code`, `country_code`, `latitude` and `longitude`. The former migration-only confidence, precision, provenance and proximity qualification columns are not part of the model.
- Proximity search includes only non-null coordinates within latitude `[-90, 90]` and longitude `[-180, 180]`; it has no qualification-status dependency.
- Public restaurant pages and JSON-LD use only `address_line1`, `postal_code` and `city_name` whenever structured fields exist; they must not expose the historical/raw `address` in that case.

## Controlled legacy migration
- Historical note: the completed pilot originally used legacy hierarchical Geography terms. The runtime schema and migration tooling now retain only `restaurants`, V2 categories/features, `restaurant_opening_hours` and `restaurant_media`.
- Each imported entity keeps its WordPress identifier (`legacy_wp_id`, `legacy_term_id` or `legacy_attachment_id`).
- `legacy:migrate-restaurants` is read-only on `legacy_wp`; its default and `--dry-run` modes write no V2 data. `--apply --limit=10` is the only currently supported write mode.
- Missing gallery, hours, coordinates or location terms are reported record-by-record. Hours are only persisted when ListingPro contains an enabled, parseable value; no schedule is inferred.
- The 2026-08-25 legacy audit found no enabled, parseable ListingPro schedule across 7,704 listings. `restaurant_opening_hours` remains ready for future/admin-entered schedules, but no legacy record is inserted until a real source format exists.

## Public page
- Fast server-rendered content.
- Core info and opening hours. When validated hours are available, the public detail page renders a compact semantic schedule card with one stored day per row, separated services and an unobtrusive current-day highlight. A server-side `Europe/Paris` status is displayed only when the current schedule is complete: open (with closing time), between services (with the next opening time), closed for the day, or the next opening on the following day. Incomplete schedules retain their available display but never produce a guessed live status.
- categories/features.
- Services are rendered as local inline SVG icons plus their text labels. The icon set has no external dependency; three domain-specific icons (halal certification, prayer room and decor) follow the same 24px rounded-stroke style as the Lucide-derived icons.
- Reviews approved by moderation and their aggregate where valid; public reviews are displayed from newest to oldest.
- map/geographic JS loaded only when needed.
- External outbound actions are limited to the existing `Site web`, `Instagram`, `Facebook` and `TikTok` destinations. The public page renders only active, http(s)-valid values from `restaurant_outbound_links`, in that order, as local-icon submit buttons in CSRF-protected forms posting to the sole opaque `/sortie` route; each form uses `target="_blank"` and `rel="noopener"`. Each form carries only its opaque token; no tokenized `/sortie/{token}` URL is exposed as an HTML link and no GET outbound route exists. The raw destination never appears in HTML, attributes, JSON-LD or embedded JavaScript. The server validates the token, records the aggregate click and issues an external GET redirect with HTTP 303. A proposal retains destinations inactive while pending; publishing activates valid supported destinations. A change made by an authorised restaurant manager or Filament administrator to a published record activates that supported destination immediately.

## Admin/owner
Create/edit/moderate records, media, hours, categories/features and ownership/claims with clear auditability. The authenticated account editor is a single shared Blade surface for every identity that `RestaurantPolicy::manage` still represents: approved claimant, exact historical author or depositor without an approved competing claim. It delegates to `ManagedRestaurantUpdater`, `RestaurantHours`, `RestaurantLocationService` and `RestaurantMediaManager`, rather than maintaining role-specific writers. It exposes name, safe description, halal meat/chicken, categories, features, ordered multi-slot hours, selected address/GPS adjustment, phone, opaque external destinations and photo cover/gallery. It deliberately excludes `contact_email` (the one account reference address), slug, status, SEO, moderation/duplicate data, claims/submissions and technical/legacy fields. Missing sections in a direct partial request remain unchanged; a complete browser submission can intentionally clear the corresponding editable value/relation.

The account editor and public proposal reuse the same Blade components for halal cards, specialty/service selection, the seven-day hours editor and pending-photo picker. The canonical hours payload is the same seven rows (`day`, `closed` / `all_day` / `slots`, ordered slots) and is validated by `RestaurantHours` in both flows; there is no owner-only hours UI or client logic. Selecting `Horaires` reveals a protected first range, further ranges are explicitly added/removed (up to eight), and copied values preserve the target day’s own persistence identifiers. The shared photo picker retains a browser-local file list across successive selections, synchronizes it to the multipart input with `DataTransfer`, previews only valid JPEG/PNG/WebP files that are at least 800 px wide and at most 10 MiB, and releases every object URL when a file is removed or the picker is destroyed. It never uploads before the normal form submit; server validation and `RestaurantMediaManager` remain authoritative. Existing owner media use a deferred `Retirer`/`Annuler` state: the hidden `remove_media_ids[]` input is enabled only when a card is marked, and removal is applied solely by the normal save. The first/last unavailable ordering action is hidden while the order and first-photo cover rule remain unchanged. A complete account-editor submission requires at least one specialty and one service server-side as well as in the progressive UI: the shared client synchronizer applies the native `required` constraint only while its entire checkbox group is empty, so an existing selected option never blocks an untouched edit. The Filament restaurant editor remains its own back-office surface backed only by `restaurant_opening_hours`.

## Public restaurant proposal

`/ajouter-un-restaurant` is a `noindex,nofollow` public Blade form. It requires no account and is protected by Laravel CSRF, server-side validation and an e-mail/IP rate limit. JavaScript progressively enhances the five-step interaction; it is required solely for the mandatory remote address-selection control. Its desktop shell uses the shared 1,120 px maximum with a deliberately compact 280 px step sidebar; the form column receives the remaining space and stacks below 840 px.

- Step 1 requires a restaurant name and at least one of `has_halal_meat` or `has_halal_chicken`; it performs an informative name-similarity lookup immediately below the name field.
- Step 2 uses the reusable Géoplateforme address-selection service in the public form, Filament and owner-edit flow. Selecting a suggestion is mandatory: it is resolved again server-side and supplies `address_line1`, postcode, city, INSEE code, country and valid GPS. Its opaque server-side token remains valid for two hours, accommodating a complete public proposal. The public UI never exposes the provider, INSEE code, administrative/geocoding details or manual postcode/city/GPS inputs. When the precise address is missing, the contributor selects the closest suggestion then moves the marker; this changes only latitude/longitude, does not reverse-geocode and never rewrites the selected structured address. Returning to this visible step preserves its single Leaflet map, marker and selected coordinates while recalculating the map size. If a rare expired selection is rejected by the server, the contributor is returned to step 2 and sees the local instruction to select the address again. Similarity candidates combine name, structured address and a 250m GPS radius without automatic merge or publication.
- The same `DuplicateRestaurantDetector` powers the informative browser lookup, the final POST and the BO. The final POST always resolves the selected address and runs it before its creation transaction: JavaScript, a previous lookup, or the normal form cannot bypass it. An active (`published` or `pending`) record is a certain duplicate only when the structured street and locality (INSEE code, or CP + city fallback) match and the normalized name matches/is very close, or the normalized phone confirms that same address. Case, accents, punctuation and minor name variants are normalized. A certain duplicate creates no restaurant, submission, media, user or e-mail; the form identifies the existing public listing and offers its claim path when claimable. A same address with a clearly distinct name (including food courts), a nearby similar name, or a same-phone locality conflict is only potential: the proposal remains `pending` and `restaurant_submissions.duplicate_signal` plus auditable candidate/reason details are shown and filterable in Filament. An archived or soft-deleted historical match never blocks a new proposal; a strong match is retained as a potential moderation signal. Same-brand records at distinct, non-nearby structured addresses are not a duplicate signal.
- Step 3 requires at least one category/type of cuisine and at least one service/characteristic (a halal certification itself remains optional), then accepts optional contact data and private outbound destinations. On desktop, the phone field occupies its own row at exactly the same width as one Site web/Instagram column; Site web and Instagram follow on one row, then Facebook and TikTok on the next. Its seven compact day rows share the account editor’s `closed` / `all_day` / `slots` component: the first range appears only for `Horaires`, extra ranges are added/removed explicitly, the non-overlap validation is shared and all values survive step navigation. Native time controls retain a fixed readable width (including their native picker icon); on constrained screens their rows wrap without horizontal overflow. The compact copy control duplicates the selected state and ranges from one source day to one or more destination days. External destinations are stored inactive until approved; active public actions use only CSRF-protected POST `/sortie` forms with opaque tokens. The external destination never appears in public HTML, DOM attributes, JSON-LD or embedded JavaScript; the server records the aggregate click then issues the external redirect with HTTP 303.
- Step 4 requires exactly one cover upload and accepts at most ten additional JPEG, PNG or WebP uploads. Its cover introduction is « Ajoutez une belle photo de couverture du restaurant. »; complementary photos are not labelled as optional, while remaining optional by contract. The contributor receives immediate local previews marked `Nouvelle`, can remove or reorder gallery files before submission, and successive selections accumulate rather than replace earlier files. Every uploaded image, cover or gallery, must be at least 800 px wide and no larger than 10 MiB. Files are revalidated by the server-side V2 media pipeline before a pending media relation is created.
- Step 5 is titled `Vos informations` (`Étape 5 sur 5`) and does not render a proposal recap. It asks whether the contributor is the manager/owner using two accessible full-width radio cards, in DOM and visual order `Non`, then `Oui`. `Non` preserves the simple contribution flow and shows only the e-mail. `Oui` reveals name, company, a normalized and Luhn-valid 14-digit SIRET and a certification checkbox alongside the e-mail; these four fields carry visible required markers and native browser validation before submission, in addition to server validation. An invalid entered SIRET is reported without a request/reload by the sole plain-French message directly beneath that field; server fallback uses the same wording and placement. Every server fallback error for an owner field is rendered directly beneath its control; the public proposal page never renders the shared global error list or a technical `validation.*` key. If server validation finds an earlier-step error, the form returns directly to that first erroneous step instead of retaining the client’s prior step number. No identity document or password is requested at this stage. Successful e-mail confirmation creates or attaches the unique case-insensitive contributor account. A seven-day, one-use password-activation link is sent only when that account is not yet normally connectable; an existing active account is only attached and receives no new activation token or link. The account can manage only its submitted restaurant(s), including while moderation is pending. This access is not a `restaurant_claim`: the restaurant remains claimable by its actual manager. After submission, the noindex confirmation screen states that the address must first be confirmed, that it will never be published, and only then can the team examine the proposal.

## Claims and removal requests

An existing published restaurant is claimable only when it has neither an approved owner nor a pending claim. Claims are authenticated, rate-limited and require declared name, company, normalized/validated SIRET, certification, and a JPEG/PNG/WebP identity-document photograph (10 MB maximum). The document is stored only on Laravel's `local` private disk under `claims/identity-documents`, with a generated path. It is never included in public markup, JSON-LD, APIs or e-mail; only an authorized admin can stream it through the authenticated document route. Documents are retained with their claim in V1; a retention policy must be reviewed before any automated deletion is introduced.

Existing-record claims have two paths: a guest first dossier needs the complete identity information, one private identity photo and expiring e-mail confirmation before manual review; an authenticated but not-yet-verified-restaurateur user submits that same complete dossier against the active session identity and enters manual review directly; a verified restaurateur submits only a certification and stays manually reviewed. A guest first approval sends a password-activation link only when it created/attached a non-connectable account; approving a dossier already attached to an active session reuses that exact account without activation. One verified account may manage several restaurants. A user who currently has the shared listing-management right (approved claim, approved `new_submission`, still-authorized depositor, or exact historic authorship) cannot delete or unpublish a restaurant. They can create only one `pending` removal request at a time with a mandatory reason and an optional comment; the listing status is unchanged until an administrator accepts it. Acceptance marks the request `approved` and uses the existing non-destructive `archived` status. Rejection marks it `rejected`, leaves the listing unchanged and may retain an internal administrator note. The three requester lifecycle notices (received, accepted, refused), and the optional operational alert, use the central queued transactional-mail journal.

A valid proposal creates a normal `restaurants` record with `status=pending`, never `published`, plus one private `restaurant_submissions` record for the contact/audit context. A new submission is `pending_email_verification`, stores only a hashed one-use verification token and has a 24-hour signed verification URL. It is not eligible for publication. A successful verification moves it to `pending_admin_review`, queues the depositor confirmation and alerts the configured operational recipient; it does not publish the restaurant. Only a `pending_admin_review` proposal may move the restaurant to `published`, which atomically marks its submission `published` and queues the public-listing notification. From that same stage, an administrator may instead refuse it: the submission becomes `rejected`, with an optional administrative reason, rejection timestamp, actor and opaque contestation reference retained for audit. The associated restaurant remains a non-public `pending` row and is never deleted. A refused proposal cannot be refused or published again, cannot retain a usable contributor activation link, and gives its depositor no listing-management access. The Filament table exposes and filters the proposal stage, displays the refusal details, and offers publication/refusal actions only while the proposal is reviewable. Historic proposals, which predate this proof, remain explicitly marked `pending_admin_review` or `published` during the forward migration and receive no retrospective e-mail.

## Référentiel de spécialités

Les spécialités sont un référentiel V2 partagé : elles alimentent les filtres de l’annuaire, l’autocomplétion de recherche, le formulaire public, et la sélection d’administration. `Burger`, `Brunch` et `Grillades` sont des entrées natives V2 disponibles même lorsqu’aucune fiche publiée ne leur est encore associée.

Les illustrations associées aux spécialités peuvent servir de miniature de secours uniquement pour les fiches sans photo raster. Elles suivent l’ordre alphabétique déterministe de la première spécialité, sans remplacer ni créer une couverture de fiche.

## SEO par fiche
Les nouveaux slugs de restaurant sont générés côté serveur de façon déterministe : nom, puis `nom-ville`, puis `nom-ville-code-postal`, puis un suffixe numérique stable si toutes ces variantes sont prises. La vérification inclut la Corbeille afin qu’une ancienne URL ne soit jamais réattribuée à une autre fiche. Les slugs historiques ne sont jamais régénérés en masse. Dans Filament, le nom remplit le slug uniquement à la création ; l’administrateur reste le seul à pouvoir modifier ce champ manuellement. Un changement volontaire de slug crée une règle exacte 301 de `/resto/{ancien-slug}` vers `/resto/{nouveau-slug}` ; une règle source incompatible ou une boucle bloque le changement sans modifier les redirections existantes.

Le panneau SEO permet de sélectionner les directives `robots` actives (`all`, `noindex`, `nofollow`, `none`, `nosnippet`, `indexifembedded`, `noimageindex`, `notranslate`) et de paramétrer les valeurs qui nécessitent un argument : longueur d’extrait, aperçu image, aperçu vidéo et date `unavailable_after`. Les directives sont rendues dans la balise `meta name="robots"` de la fiche ; une fiche `noindex` ou `none` est exclue du sitemap.

Le titre par défaut d’une fiche publiée est `Restaurant {nom} Halal à {ville} spécialité {première spécialité}`, sans suffixe de marque. La première spécialité est déterminée par ordre alphabétique pour un résultat stable. Une valeur renseignée dans `seo_title` remplace intégralement ce titre généré.

## Non-régression

Une modification sans rapport dans Filament doit préserver les relations existantes de la fiche : médias, catégories, services, zones, avis, horaires et adresse/GPS. Le registre V2 de sentinelles compare ces relations exactement sur préproduction et bloque toute perte inattendue.
