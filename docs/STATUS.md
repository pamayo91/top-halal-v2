# Top-Halal V2 — Status

Last updated: 2026-09-20

Latest restaurant detail spacing correction: the gap from `Informations` to the `Avis` title now exactly matches the existing gap from the end of `Avis` to `Une information à corriger ?`; the reference gap and all other restaurant-detail spacing remain unchanged.

Latest restaurant report-form presentation: the public `Une information à corriger ?` block now opens the existing report flow inside the same compact, lightly tinted card system as the review form. It keeps its title, toggle, route, validation, identity verification, anti-spam and submission behavior unchanged; the textarea is a matching reasonable height, the desktop action aligns right, and the mobile card stays within the normal page width with a full-width action.

Latest editorial mobile correction: wide tables within article prose now scroll inside their own mobile container rather than widening the whole page. Their content and desktop layout are unchanged; the preproduction mobile article sentinel asserts that the document itself has no horizontal overflow.

Latest public saffron finishing pass: every public form field now replaces browser-blue focus with the same accessible saffron border/ring; custom restaurant-search keeps one outer saffron ring and rating radios keep their visible star-label focus. Buttons and links are unaffected. The footer's existing full-width divider is now a perceptible but attenuated saffron line; the footer surface and links are unchanged.

Latest public saffron second pass: saffron remains limited to its two existing tokens and is now perceptible through short editorial rules on existing home sections and eligible surtitles, a small article-category marker, a sidebar editorial marker, quotation bar and accessible text selection. Contact reuses its existing decorative kicker rule in saffron. Restaurant interactions, submission progression, claims, forms, navigation, links, CTA, focus, statuses and semantic chips remain green or neutral; restaurant detail adds only its small non-functional intro rule beside the existing rating stars. A reusable `badge-accent` style is available solely for a real editorial/positive label, without manufacturing any business badge.

Latest public saffron reinforcement: the shared breadcrumb component now marks only its visual separators in saffron; labels and links are unchanged. Every real editorial-sidebar title receives the same 26 × 2 px saffron rule, and city speciality/service/nearby-city sections share the 36 × 2 px home-section rule. The article quotation bar remains 3 px saffron. No interaction, semantic status or restaurant-service colour changed.

Latest regression-sentinel correction: restaurant sentinels now snapshot only the relations required by their explicit role. Categories, single-media and structured-address sentinels no longer compare live reviews, while the review sentinel protects its selected historical review by ID, restaurant relation, legacy identity and status without treating an additional legitimate V2 contribution as a failure. No restaurant or review data was changed.

Initial public saffron accent: the local design system now exposes only `--color-saffron` (`#D9A441`) and `--color-saffron-soft` (`#F8F0DD`) as a decorative/valuation accent. The first pass used it only on restaurant review stars (summary, published cards and native rating choices); nearby values remain neutral and focus rings, buttons, links, navigation, form controls and success states remain green. No unsupported restaurant/editorial promotion badge was introduced.

Latest public background foundation: `--page-background` remains `#fbfaf7`, while the default public surface is now a non-repeating 24-rem vertical gradient from `--page-background-start` (`#f7f7f1`) to `--page-background-end` (`#fbfaf7`). It is applied at `html`/`body` and finishes on the reference color, preventing a repeat or seam on long pages. Account, owner editor, restaurant submission and 404 use that foundation; intentional component/section fills remain local. Contact and claim keep their own decorations untouched. Desktop/mobile checks passed across public home, directory, geographic, facet, restaurant, editorial, blog, article, submission, login and Contact families; Contact retains its radial-only rendering.

Latest restaurant-review ownership and presentation correction: review eligibility is now the inverse of the central active-representation query, not ownership alone. An approved owner, `new_submission` manager, historical author, or a still-authorized non-manager depositor cannot review that same restaurant; the latter keeps its distinct status, temporary editor right and claim lifecycle unchanged, and can review a restaurant it does not manage. The central policy hides the conflicted form and rejects a direct POST before any opinion, verification, notification or delivery log exists. Review-author prefill uses only explicit human claim/submission identity for restaurant-linked accounts, never their commercial account/listing name; unavailable identity stays blank. Published review cards use the shared saffron rating stars, preserve non-empty historical titles, and the approved-only summary is compact (`5/5 · 1 avis`, up to one useful decimal) without changing AggregateRating. The real preproduction regression relation is a published `customer` submission without an approved claim; targeted PHP passes (20 tests, 111 assertions), review Playwright desktop/mobile passes (4/4), and the complete regression gate passes with no new Laravel-log error.

Latest operational contribution alerts: reviews and editorial comments now queue one central, tracked operational e-mail only when a new contribution actually enters `pending` moderation—after e-mail confirmation where needed, immediately for an authenticated/proven identity. Two administrable templates provide filtered back-office CTAs; the persisted delivery-log reference makes the business alert idempotent across repeat confirmation links, repeated calls and queue retries. URL-free validation, identity proof, token and moderation behavior are unchanged.

Latest review e-mail confirmation UX: review form outcomes now return to the stable `#avis` anchor with a CSS scroll margin, so the important contextual status is visible instead of landing at the top of the restaurant. A valid review-verification page now confirms the e-mail, accurately says the pending review awaits validation, and uses a server-derived `Retourner au restaurant` CTA to the same restaurant’s `#avis`; the return displays the matching post-confirmation message without the old verification prompt. Used review links have a non-misleading idempotent message, while unavailable targets close safely. The shared hashed-token, expiry, single-use, anti-usurpation, session-proof and moderation rules remain unchanged. Preproduction targeted PHP passes (24 tests, 154 assertions), review Playwright desktop/mobile passes, and the complete 80-check regression gate passes with no new integrity error.

Latest restaurant-review micro-polish: 26 px star glyphs are now visually grouped using preserved 44 px overlapping targets, with continuous hover preview and exact temporary labels that restore the selected radio value on mouse leave. The empty-state invitation is one naturally wrapping responsive sentence, with only the restaurant question semibold. Targeted PHP passes (15 tests, 79 assertions), desktop/mobile review Playwright passes, and the complete 80-check regression gate passes with no new Laravel-log error.

Latest restaurant-review finish: star controls now follow the same natural 1→5 order in the DOM, keyboard, submitted value, visual fill and label (`Décevant` through `Excellent`); clipped native radios no longer overlap/capture each other’s clicks. The zero-review state omits redundant `0 avis`, the rating appears before identity, the open disclosure hides its redundant CTA, and star/help/button spacing was tightened without changing the modest tinted form treatment. Targeted preproduction PHP passes (15 tests, 77 assertions), desktop/mobile review Playwright passes, and the complete 80-check regression gate passes with no new Laravel-log error.

Latest restaurant-review UX: the public review area now gives an empty restaurant a compact community invitation, while the disclosure keeps its native no-JS behavior. The new form removes the title field from the validation/payload contract without deleting historical data, uses keyboard-operable native 1–5 star radios with dynamic CSS labels, calls the visible author field `Prénom ou pseudo`, and arranges identity/e-mail as responsive desktop columns. Field-level errors reopen the form and preserve a valid selected note; verified identity, anti-usurpation, manager prohibition, moderation and approved-only aggregate behavior are unchanged. Preproduction targeted PHP coverage passes (14 tests, 73 assertions), review Playwright passes on desktop/mobile with no console error, and the complete 80-check regression gate passes with no new Laravel-log error.

Latest depositor-identity correction: confirmation of a non-manager public proposal no longer uses the restaurant name as the required `users.name` fallback. The authenticated first-claim form rejects that recognizable historical fallback at runtime, leaving the required personal-name field blank and displaying the session e-mail alone; a name explicitly supplied in the dossier is retained on the existing account. The connected-only card is now titled `Vérifiez votre identité`; guest cards remain unchanged.

Latest authenticated first-claim correction: an active non-manager depositor is no longer presented with a login card when claiming a restaurant they submitted. The central claimability rule still treats that submission as non-ownership; the session user sees the full first dossier only, with a read-only session e-mail, and the server ignores forged posted identity fields. The claim enters manual review attached to the same user; approval grants the existing account ownership without creating an account or sending a password activation, while refusal leaves depositor rights untouched. Targeted PHP coverage includes guest, depositor, forged-identity, verified-restaurateur, owner/history/active-claim and approval paths.

Latest restaurant outbound-links revision: restaurant actions now use native CSRF-protected `POST /sortie` forms with an opaque hidden token and local-icon submit buttons. They open the external destination in a new tab via native `target="_blank"` plus `rel="noopener"`. The legacy public `GET /sortie/{token}` route is removed, and neither that tokenized URL nor any destination URL appears in public HTML, JSON-LD or JavaScript. The server preserves aggregate counting and uses HTTP 303 to continue externally in GET.

Latest new-photo-panel visual refinement: the owner editor’s dashed `Nouvelles photos` panel has content-driven height only. Its desktop padding is 20 px (16 px on compact mobile), inherited paragraph margins are reset locally, and the heading, picker, count, two help lines and eventually gallery use the compact 6–16 px rhythm without changing media, upload, preview, validation or file-list behavior. The complete preproduction regression gate passes all 80 browser/sentinel checks with no new Laravel log error.

Latest owner-media initial-state correction: the generic card paragraph `display:flex` rule was overriding the pending-removal notice’s native `hidden` state, even though every `remove_media_ids[]` input was disabled. The card now explicitly honors every hidden control, and the JS initializer resets every existing card to preserved/disabled before it accepts an explicit `Retirer` action. A marked card alone enables its media ID, shows `Suppression prévue`/`Annuler`, and hides its reorder actions; cancel restores the position-appropriate actions. Targeted isolated PHP passes (11 scenarios, 120 assertions), including three initial photos with zero pending removals, a save of another field that preserves all three, and removal of only the selected second photo. The complete preproduction regression gate passes all 80 browser/sentinel checks with no new Laravel log error.

Latest editor taxonomy validation correction: a complete untouched owner edit no longer fails native browser validation when a saved category or service is checked somewhere other than the first checkbox. The shared taxonomy synchronizer now makes only an entirely empty group required, on both the public proposal and account editor; server validation remains unchanged. Isolated PHP coverage passes (8 tests, 97 assertions), including the exact preselected non-first option regression; the complete preproduction regression gate passes all 80 browser/sentinel checks with no new Laravel log error.

Latest restaurant photo-picker UX: `Ajouter un restaurant` and `Modifier la fiche` now use the same Blade photo-picker and browser module. The owner editor separates persisted cover/gallery media from `Nouvelles photos`, provides an immediate local thumbnail, filename, `Nouvelle` badge and `Retirer` action for each pending upload, and preserves every valid file across successive selections until the normal multipart save. The shared picker mirrors the existing server contract (JPEG/PNG/WebP, minimum 800 px wide, maximum 10 MiB, maximum ten gallery/new files), rejects invalid files visibly before they join the list, uses/revokes object URLs correctly and does not upload before the save action. Existing ordering/removal and every policy, media and ownership rule are unchanged. Targeted PHP passes (39 tests, 392 assertions); public photo browser coverage passes on desktop and mobile, and the complete preproduction regression gate passes all 80 browser/sentinel checks with no new Laravel log error. The new managed-editor browser coverage validates accumulation, pre-save removal, invalid dimensions, multipart persistence and removal of the `Nouvelle` state after reload whenever the isolated preproduction fixtures are supplied.

Latest restaurant editor UX refactor: the account editor and public restaurant proposal now consume shared Blade components for halal cards, specialty/service selection and the canonical seven-day multi-slot hours editor. The owner-only `owner-hours-*` markup and behavior are gone; one shared JS module handles closed, all-day, one-or-more ranges, add/remove, copying and overlap validation. The editor uses a 1,120 px responsive shell, two-column contact/social layout, three-column desktop media thumbnails with variant-correct dimensions and a sticky save bar. Business policy, updater services, ownership/claims, address/Géoplateforme, media security and account e-mail rules are unchanged. Targeted PHP coverage passes (38 tests, 378 assertions); Blade cache, Vite build, JS syntax checks and the regression/admin sentinel subset (25 tests, 126 assertions) pass. The isolated full PHP suite has one pre-existing environment-only `LegacyUserMigrationTest` failure because the temporary checkout has no legacy source configuration. The deployed preproduction regression gate then passed all 80 browser/sentinel checks with no new Laravel log error.

Latest account e-mail change: `Mon profil et sécurité` now presents distinct Address e-mail and Password rows. A connected user submits a unique RFC-valid new address and the current password; the three-per-hour per-account/IP limiter queues a 24-hour signed one-use link whose random token is stored only hashed. The account address stays unchanged until confirmation, then the transaction updates the same `User`, marks the new address verified and synchronizes `restaurants.contact_email` only for listings still represented by that user through the central policy. Historical submission/claim e-mail snapshots, user IDs, permissions and transferred former-depositor listings remain untouched. The former address receives a queued security notification; delivery logs retain no token or verification URL.

Latest restaurant account editor: the prior four-field editor has been replaced by one policy-gated business editor for currently authorized depositors, approved claimants and exact historic authors. It reuses the canonical hours, location and media services to edit general data, halal flags, specialities/services, seven-day multi-slot hours, selected address or GPS-only marker refinement, phone, opaque outbound destinations and ordered cover/gallery media. E-mail remains account-only; moderation/status/slug/SEO/legacy and relationship-audit data are excluded. A focused audit is retained in `docs/generated/restaurant-editor-audit-2026-09-18.md`; PHP coverage verifies the three rights paths, actual hour/media/GPS persistence and preservation of unrelated administrative data. Preproduction Playwright passed all eight desktop/mobile flows on isolated depositor, claimant and historic-author fixtures, including a real hours/media save; fixtures were then removed. The complete regression wrapper also passed with no new Laravel log error.

Latest public restaurant-submission address-token correction: the selected opaque address token remains usable for two hours rather than 15 minutes. If it nevertheless expires or is cleared before final validation, the response returns directly to step 2 with the local « Cette suggestion a expiré » guidance, rather than leaving the user on step 5. Feature coverage forces an expired token and verifies this exact recovery.

Latest public restaurant-submission server-validation routing: when the final POST discovers an error from steps 1–4, the server now returns the contributor to the first affected step instead of retaining step 5 and hiding the error. Owner-field errors remain on step 5 directly beneath their relevant field.

Latest public restaurant-submission server-error presentation: the page no longer shows Laravel’s global error list. Owner full name, company, SIRET and certification fallback messages render only below their individual control and now all have explicit French text; technical `validation.*` keys cannot be exposed publicly. Browser coverage also submits a complete owner proposal, proving that the valid owner path reaches the confirmation screen without a server validation error.

Latest public restaurant-submission SIRET validation: an invalid owner SIRET is now caught in the browser before any POST/reload and displays only « Le SIRET doit comporter 14 chiffres valides. » immediately under that input. The server fallback now has the same explicit French rule and field-local rendering, so no `validation.digits` key can reach the public page.

Latest public restaurant-submission owner-field presentation: every required marker now stays on the same line as its Name, Company or SIRET label, rather than becoming a separate grid row. Desktop/mobile browser coverage checks this layout as well as the existing required validation.

Latest public restaurant-submission owner validation: selecting `Oui` at step 5 visibly marks Nom / prénom, Société, SIRET and the certification mandatory and applies native required validation before the request is sent; returning to `Non` removes these client constraints. The existing conditional server validation remains the authoritative fallback.

Latest public restaurant-submission final step: Step 5 is now `Vos informations` while preserving `Étape 5 sur 5`; its proposal recap is no longer rendered by Blade or JavaScript. The accessible role cards are ordered identically in the DOM and visually as `Non`, then `Oui`; the existing conditional owner fields and e-mail help remain unchanged. The post-submit noindex page now clearly separates required e-mail confirmation from later Top Halal review, explicitly says that the e-mail address is never published, and asks visitors to check spam. The existing pending-email-verification → pending-admin-review → human publication workflow and transactional e-mails are unchanged. Desktop/mobile Playwright coverage verifies the final step, both role states, absent recap, successful submission and exact final wording.

Latest public restaurant-submission presentation: the five-step Blade form now gives its explanatory header readable secondary contrast, spans the complete usable header width with the progress track, and renders informative close-name candidates immediately after the restaurant-name field. Step 2 uses plain French with no public provider/administrative-geocoding terminology. Returning from step 3 dispatches a visibility refresh to the already-created Leaflet map; it retains the selected marker and coordinates while correctly recalculating map size. In step 3, the phone remains alone on a desktop row with exactly the width of one Site web/Instagram column, followed by Site web/Instagram and Facebook/TikTok rows. Step 4 now has a concise neutral cover-photo introduction, does not call complementary photos optional, and promises their existing removal/reordering controls before submission; its cover requirement, allowed formats, dimensions, gallery limit and order remain unchanged. Desktop/mobile Playwright coverage includes this return cycle, the mobile close-name placement and step-four cover/gallery preview, add, remove and reorder actions.

Latest public restaurant-submission hours and outbound actions: Step 3 now requires at least one category/type of cuisine and at least one service/characteristic, enforced by the progressive UI and the final server request; halal certification itself remains optional. Its hours use compact day/state rows backed by the unchanged seven-day `closed` / `all_day` / `slots` server contract. A first range appears only for `Horaires`; the second is explicitly added/removed, and state/ranges copy to selected days or survive navigation. Native time inputs are now wide enough to show the complete value and native picker, while two ranges and their remove action share a clear desktop row where space allows; the remove action moves beneath the second range in a deliberately aligned fallback. The day select, label hierarchy and copy block are proportioned more compactly without responsive overflow; the copy action occupies its own grid row, preventing collision with day choices at every breakpoint. Public approved restaurant destinations now use opaque CSRF-protected POST `/sortie` forms for Site web, Facebook, Instagram and TikTok; no external destination or tokenized `/sortie/{token}` link leaks into HTML, attributes, JSON-LD or embedded JavaScript, and the controller records aggregate clicks before a validated 303 redirect.

Latest restaurant-slug safety: new public and Filament-created listings now receive a deterministic readable slug from name, city and postcode, falling back to a stable numeric suffix while reserving slugs held by trashed records. Existing historical slugs were untouched. Filament updates never derive a slug from a renamed restaurant; only an administrator may edit it manually. A voluntary slug change creates an exact application-managed 301 from the old `/resto/{slug}` path, while incompatible existing rules or detected redirect loops block the change without overwriting any rule.

Latest BO restaurant hours: the Filament restaurant editor now has an `Horaires` tab backed exclusively by the existing `restaurant_opening_hours` relation. Its compact fixed seven-day table supports Fermé / Ouvert / 24h/24 with one or more ordered time slots; choosing Ouvert immediately reveals a first blank slot and the add action is reserved for additional slots. Expanded entries use a left-aligned max-content grid: every Ouverture → Fermeture row has the same compact control columns and fixed action column, with 8 px horizontal and 4 px vertical gaps. The add action occupies the first row’s action column; only extra rows expose deletion there. The mandatory first slot remains protected from deletion. It retains legacy row identity during edits and uses the same canonical formatter as the public restaurant-submission flow; public rendering is unchanged.

Latest public typography: General Sans is now the local public frontend typeface. One official 38 KB WOFF2 variable file (200–700) is bundled through Vite with `font-display: swap`, licensed under the retained ITF Free Font License 2.0. Public hierarchy uses 400 text, 500 metadata, 600 navigation/forms/buttons/small headings and 700 H1/major headings; the shared public typography layer replaces the former Georgia rendering on form/editorial headings. Filament remains unchanged and no external font CSS, preconnect or font request is allowed.

Latest 404 page: the public 404 now uses the shared shell, fixed noindex HTTP-404 behavior, responsive illustration/message/CTA layout and four fixed reassurance blocks. On desktop, the left illustration intentionally exceeds the content block’s visible height while remaining centered, uncropped and undistorted; mobile returns to natural single-column image flow. The reassurance area is a light, spacious 1,120 px card with 56 px pastel icons, short subtle dividers and balanced text columns; it becomes 2 × 2 on tablet and a single column on mobile. Settings exposes only V2 media, title and text overrides; empty values fall back to code defaults and the designated V2 fallback illustration.

Latest restaurant-submission refusal: an administrator can now refuse exactly one `pending_admin_review` proposal from the Filament queue, with an optional retained reason. The proposal is atomically marked `rejected`; its restaurant stays stored as non-public `pending` data, is neither published nor deleted, and the depositor loses proposal-derived management/activation access. The existing queued transactional renderer and delivery history send one no-reply notification with the reason when supplied, an explicit “merci de ne pas y répondre” notice, and a “Contester ce refus” CTA to the existing Contact form with an opaque proposal reference.

Latest public account UX: `Mon compte` now uses the shared public shell and form-era visual language: a light personal header, calculated plain-French business profile, responsive restaurant cards, French status badges, existing management/removal actions and a useful empty state. Its existing authorized restaurant query and all Policies, claims, ownership, roles and workflows remain unchanged.

Latest content-report workflow: restaurant, article and page correction reports now share the contributor identity proof. A guest creates only a one-use 24-hour verification payload until clicking the signed link; then one existing-or-new verified `User` receives the `new` report and session proof. An authenticated account or matching valid proof bypasses only that new verification and always retains its own identity. The simple Filament queue provides `new`, `in_progress`, `resolved` and `dismissed`, public-content access and reporter identity state; it never changes editorial or restaurant data. The operational Contact recipient receives the existing queued transactional renderer/history notification after report creation; reporters receive no acknowledgement/result e-mail. Authorized restaurant managers see a primary direct-edit CTA while retaining the report path.

Latest public restaurant duplicate guard: the existing shared detector now powers the final POST as well as the informational browser lookup and BO checks. A live exact structured-address + normalized-name (or phone-confirmed) match rejects before any restaurant, submission, media, user or e-mail write, returning the public listing and claim path where available. Same-address different names, nearby similar names and historic archived/trash matches remain allowed `pending` submissions with persisted, filterable BO duplicate signals; distinct branches of a chain do not block one another.

Latest restaurant-removal completion: every currently authorized listing manager (approved owner claim including `new_submission`, still-authorized depositor, or exact historical authorship) can open only one `pending` removal request with a required reason; direct deletion remains unavailable. The listing stays unchanged during review. The Filament moderation queue archives it non-destructively on approval or records a rejection and optional administrator note without changing the listing. All requester lifecycle e-mails and the optional operational alert use the existing queued transactional renderer and delivery history.

Latest restaurant-submission activation guard: e-mail confirmation resolves one `User` case-insensitively and always attaches the new submission. A new account, a pending account or a contributor-only identity receives an activation only when the active account is not normally connectable (`login_enabled=false` or `must_change_password=true`). A normally active account receives no token or activation link and its password, flags, role and rights are untouched. A review/comment identity retains `login_enabled=false` through confirmation and e-mail delivery; only a valid activation-link password choice enables that same `user_id`, retaining every contribution. An administratively disabled account is never reactivated by this flow.

Latest expiring-link UX: proposal, first-claim, contributor (review/comment/report) verification and eligible account-activation URLs now give a clear noindex final/expired page instead of a generic error. An expired still-eligible object can request one queued transactional replacement link (three requests/object/IP/hour); replacement keeps the recipient/payload, creates no business duplicate and atomically replaces the SHA-256 token hash, invalidating the old URL. Used links give idempotent state, while rejected/closed work and disabled accounts expose no resend; a published proposal can still resend only if its already-linked account still genuinely needs activation. Existing 24-hour verification, seven-day activation and 60-minute password-reset lifetimes are unchanged.

Latest historical contributor access correction: the 77 migrated, unactivated `user` identities whose only reliable V2 activity is a review or comment now have `login_enabled=false`. The idempotent scope excludes every claim, public submission and exact historic restaurant authorship, and changes neither roles nor restaurant/contribution data; the BO now reports their connection as disabled.

Latest restaurant-review ownership guard: the shared `RestaurantPolicy` now exposes the strict validated-manager rule used by restaurant management — approved claims (including `new_submission`) and exact historic authorships, never a simple depositor. A signed-in manager sees an explanatory message instead of the restaurant-review form; the server rejects direct POST attempts and rechecks before a verified e-mail flow can create a review. Editorial comments are unchanged. Preproduction validation passed: 22 targeted PHP tests / 182 assertions and the full 80-check browser, sentinel and Laravel-log regression gate.

Latest BO technical-access hardening: the « Accès technique » field is no longer rendered in either user create or edit forms. A BO-created account is always persisted as technical `user`, and an edit never writes the stored technical role; existing administrators retain their access but cannot promote or demote an account through this interface. PHP and browser coverage enforce the absence of the control.

Latest historical restaurateur management: every exact `legacy_restaurant_authorships` relation is now a first-class management entitlement for that linked restaurant. The historical manager sees and edits the listing from `Mon compte`; `RestaurantPolicy` grants access, and the existing relationship blocks competing first claims. No `restaurant_claim` is created and no historic relation is rewritten. The BO profile remains calculated independently of the technical `user`/`admin` access role. Preproduction scope: 90 active technical `user` accounts and 7,604 exact active links.

Latest depositor-management transfer: a public restaurant depositor may manage the listing only while it has no approved claim. Pending and rejected claims preserve that temporary right; approving a claim grants management to the claimant and removes the listing from the former non-owner depositor's account and edit routes. The historical `restaurant_submissions` row and both users remain intact. A direct edit, update or removal URL now redirects a former depositor to an explanation and Contact CTA rather than returning a raw 403. The Policy, account query and preproduction regression gate are validated.

Latest submitted-owner ownership: a confirmed public depositor who declared themselves manager/owner receives one deferred `new_submission` claim, not ownership. When Top Halal publishes the restaurant, the existing normal claim-approval path approves that claim and makes the depositor the official owner. Non-manager depositors never receive an automatic claim.

Latest contribution identity: public restaurant reviews and editorial comments now share a one-use, 24-hour signed e-mail verification workflow. An unproven submission persists only a private verification payload; successful confirmation atomically creates or reuses one `User`, marks the address verified, records a server-side session proof for that exact user and creates the actual contribution as `pending` with `user_id`. New contributor identities have `login_enabled=false`, a random unusable password, role `user`, no claim and no restaurant-management grant. An authenticated user or a valid proof bypasses only the new verification, never moderation. A known e-mail without a current proof must verify again. The daily scheduler purges only verification rows expired or consumed for more than seven days; it never removes related users, contributions or e-mail history.

Latest restaurant-owner account: « Mon compte » exposes the existing CSRF-protected logout action, which invalidates the authenticated session and returns to the homepage.

Latest restaurant-submission verification: a public proposal creates a pending restaurant plus a `pending_email_verification` submission and a tracked queued e-mail with a 24-hour signed, one-use verification link. Confirmation moves only the submission to `pending_admin_review`, queues its acknowledgement and alerts the configured operational recipient; the central publication guard rejects any earlier restaurant publication. An administrator can then publish the restaurant from the clearly labelled Filament queue, which sets the submission to `published` and queues the public-listing e-mail. Historic proposals remain in their existing moderation/published state and receive no retrospective e-mail.

Latest contributor space: every confirmed public depositor, irrespective of the manager/owner answer, is now linked to a scoped account and receives the seven-day one-use password-activation CTA in the same confirmation e-mail. The account manages submitted listings only until a real manager's claim is approved; it creates no ownership claim, so the actual manager can still claim the restaurant normally.

Latest account-entry cleanup: the generic `/register` page and its user-creation endpoint are removed. Accounts now originate only from validated public proposals or approved first claims. The former restaurant-specific registration hand-off remains only as a compatibility redirect to the selected direct claim form.

Latest email CTA fix: the shared transactional renderer now resolves `restaurant_url`, `activation_url` and `admin_url` in addition to the original action, verification and reset URLs. Restaurant-publication confirmations therefore render their configured public-listing button.

Latest ownership V2: first restaurateur claims create no account or password, require a private document and expiring e-mail confirmation before manual review, then send a password-activation link after approval. A verified restaurateur signs in and submits only certification for each manually reviewed additional restaurant. Claimability remains central and owners can request — never directly perform — non-destructive removal.

Latest claim presentation: the unauthenticated claim page uses compact Contact-aligned cards within a 1,120 px maximum: a 44/56 desktop grid with a 28 px gap, and the four first-claim details in two columns only on large screens. Below 840 px, fields and cards retain a single-column flow. The identity upload instruction and certification checkbox are independent, accessible visual blocks; business logic is unchanged.

Latest submission verification UX: step five now uses accessible full-width manager/owner radio cards with clear selected states. Choosing `Non` leaves only the contributor e-mail, while `Oui` exposes the existing manager fields and truthful follow-up wording; no submission workflow changed.

Latest submission layout: the five-step form uses the full 1,120 px desktop shell, keeping the 280 px progress sidebar compact and giving the main form column the additional space. Below 840 px it remains a single-column flow.

Latest claim UX: a guest clicking a claimable restaurant’s claim CTA sees the direct first-claim form, alongside the existing-restaurateur login form. Claimability is rechecked at form and submit boundaries.

Latest navigation: header and footer navigation are now configured from the dedicated Filament « Navigation » group. Blade retains the public structure and styling; `menus` / `menu_items` only manage labels, order, destinations, visibility and two-level submenus. The initial header is Restaurants, Villes, Cuisines, Guides and Blog, with no search icon; the account secondary CTA and green restaurant-submission CTA remain route-backed. Footer columns, legal links and valid social URLs are configurable, and public navigation is SSR with targeted cache invalidation.

Latest email/contact: SMTP-only runtime settings, encrypted SMTP-secret handling, safe registered transactional templates and a French operational delivery history are implemented with the database queue. Transactional e-mails now share the validated 620px layout: centered V2 logo or name fallback, the existing public Footer presentation as header slogan, common public design tokens, safely escaped plain-text content rendered by the shared renderer as explicit paragraphs/short breaks, a button matching the site, and a single configurable three-line footer. The preview and real mailable use this exact renderer; CTA buttons have 28 px of inline spacing above them and 40 px of inline-cell padding adds robust breathing room before the footer divider. `Emails > Configuration globale` is limited to name, optional logo and footer text; old e-mail-only colour/year/secondary-footer values are ignored and cleaned on update. Old literal `\\n` overrides are forward-normalized to real line breaks. Templates are always deliverable when their application event occurs; the obsolete activation control and database column are removed. The history is linked to the real `jobs`/`failed_jobs` records and provides safe details, filters, counters, contextual retry/cancel/release actions and explicit sent/failed/cancelled/expired states; it does not offer accidental resend of delivered e-mails. A daily controlled retention Cron purges only cancelled/expired entries after 60 days, while sent and failed records remain audit history. On o2switch preproduction, user Cron runs every minute through a non-blocking `flock`-guarded finite worker; it reconciles orphaned pending journal rows first, then uses `retry_after=90`, a 75-second worker timeout and job-level `30/120/300` progressive backoff. The contact end-to-end flow is validated: the public form returns immediately, its job waits in queue, Cron sends it automatically, the delivery history becomes `sent`, no worker persists and `failed_jobs` remains empty. Verification, password reset/change and claim emails share the same queued policy. `Emails > Configuration > Envoyer un e-mail de test` remains synchronous without a queue job, confirms SMTP acceptance in the BO or returns/logs an actionable sanitised error, and never exposes credentials. The transport selector, obsolete primary-administrator address and unused introduction field are absent; the recipient, acknowledgement option and confirmation message are managed only in `Contact > Réglages`.

Email rendering update: delivered CTA buttons use the e-mail-safe table/cell `bgcolor` pattern with 28 px of inline top spacing, plus a VML rounded-button fallback for Outlook’s Word engine; the 620 px transaction block has 12 px rounded top and bottom cells and separated table borders, preserving the rest of the approved layout.

Latest editorial sidebar UI: the existing SSR/Filament sidebar configuration now renders as a differentiated, compact editorial rail. Desktop gives the article a 70/30 layout; related articles and restaurants use responsive V2-media thumbnails, the local-search, correction, proximity, exploration, sharing and contact blocks each have a distinct lightweight treatment, and mobile keeps only the TOC before the article. The proximity CTA requests browser location only after an explicit click and falls back to the existing directory search.

Latest back-office media: restaurant media previews now preserve the whole image rather than crop it or upscale it: their HTML dimensions match the delivered variant (480 × 270 for restaurant 7699), rather than the 2880 × 1620 original. The first ordinary photo is visibly labelled as the cover, while explicit up/down controls persist and audit gallery order changes; specialty fallback thumbnails remain separate and cannot become a cover. An editor may now directly add JPEG/PNG/WebP photos to a restaurant, then promote a photo by ordering or detach it without deleting its V2 media-library asset; an unnecessary specialty fallback can be detached too. The authenticated Playwright flow verifies the reported restaurant (ID 7699) exposes those controls without browser errors.

Latest cleanup: the obsolete legacy Geography runtime has been removed after a preproduction data/dependency audit. `locations` (1,971 legacy-term rows) and `restaurant_location` (7,613 historical links) contained only legacy identifiers, hierarchy and labels; all linked restaurants have structured `city_name`, while the 168 city-slug cutover redirects (including two approved homepage fallbacks) already live independently in `redirect_rules`. Public city, administrative, nearby-city and sparse facet systems remain exclusively structured-data/resolver based. The forward-only migration removes both tables; historical create/cutover migrations remain for migration history only.

Latest Quick audit: completed on preproduction with no restaurant business write detected. Quick's official server-rendered Next.js directory payload yielded 198 restaurants, all explicitly marked halal in the per-restaurant source rows; 2 are exact V2 matches, 7 are update proposals, 3 remain probable, 186 are missing, and no duplicate or scrape error was found. The reusable command retains its raw snapshot privately under `storage/app/audits/quick/`; versioned summaries are `docs/generated/quick-restaurants-audit.csv` and `.md`.

Latest Quick sync: 186 official Quick restaurants were created and 12 reviewed matches (including the three formerly probable matches) were refreshed on preproduction. All 198 are assigned to existing `Fast-food`, with only existing feature mappings; all retain the official ARGML sentence. The immediate second run created and changed zero restaurant rows.

Latest Quick media: the reviewed Quick logo is normalized without cropping to a 1440 px maximum WebP original, with the regular 480/960/1440 WebP derivatives. It is associated as normal first gallery media (not a fallback) to the 198 current official Quick records, so the public cover/card and back-office media management use the standard path.

Latest back-office timezone: Filament explicitly presents and edits date-times in `Europe/Paris`, including daylight-saving transitions. Laravel technical instants remain UTC; no existing V2 data was rewritten.

Latest: city + service SEO facets now reuse the sparse specialty-facet engine and the existing `restaurant_feature` relation. `city_service_seo_pages` stays empty until an administrator configures a combination; only open rows become canonical, indexable, sitemapable SSR landings and city links. The shared Facettes SEO screen switches between Specialty and Service opportunities without rendering a city × term matrix. The V2 category/service slug audit found no collision.

Latest geographic SEO: city identity is now the canonical INSEE `city_code`, with `city_name` retained for display. Homonyms use a precise department-suffixed URL and their former unsuffixed page is a noindex disambiguation page. The local administrative resolver adds region and department pages, direct internal links, canonical visible/JSON-LD breadcrumbs and sitemap eligibility without legacy Geography or extra restaurant columns. The preproduction audit detects 1,899 city pages, 97 departments and 17 regions; at the global threshold of 5, 297 city, 93 department and 16 region pages are indexable. The three real city/department collisions are resolved automatically as `indre-36`, `mayenne-53` and `vienne-86`, while Paris and equivalent DOM territories remain single pages. The two migrated SEO overrides remain sparse, on canonical Marseille (13055) and Paris (75056) codes. City pages now also expose an SSR « Villes aux alentours » block from locally persisted official commune centres, without restaurant-coordinate derivation or runtime remote calls; its global radius and result cap are configurable in Settings and invalidate the cache cleanly. City + specialty facets are now independently sparse and closed by default: only explicit `open` configurations are landing pages, sitemapable and linked from their city; filters remain discovery controls for every other combination.

Restaurant details now reuse those same city and administrative resolvers for their visible breadcrumb and `BreadcrumbList` JSON-LD: `Accueil > Restaurants > région > département > ville > restaurant`, with precise homonym URLs, resolver-decided department collisions and no redundant Paris/DOM level. Restaurant canonicals and geographic routes are unchanged.

Public-launch prerequisite: the first comment or restaurant review from an e-mail address must be confirmed through a signed, expiring link before that contribution can be approved. The current pending/moderation workflow alone does not prove ownership of the submitted e-mail address.

Latest admin validation: the dedicated preproduction test administrator validates authenticated Filament flows on desktop and mobile. The City SEO table search, editor opening and override saving are covered; save audits retain only structured city metadata.

Authenticated BO test access: the local-only `PREPROD_ADMIN_EMAIL` / `PREPROD_ADMIN_PASSWORD` environment variables designate the dedicated preproduction test administrator. They are mandatory for authenticated Playwright coverage and are never stored in Git.

| Area | Status | Notes |
|---|---|---|
| Architecture | DECIDED | Laravel 13 / PHP 8.4 / Apache / MariaDB / no Docker |
| Restaurations/imports legacy | DECIDED | Toute restauration BDD, reconstruction/réensemencement V2 ou import/réimport legacy exige deux confirmations explicites distinctes du propriétaire dans la conversation courante : cadrage puis feu vert immédiat avant écriture. |
| Starter documentation | DONE | Initial Codex handoff created |
| Legacy SQL inventory | BASELINE | Must be reproduced by migration tooling |
| htaccess redirect inventory | IN PROGRESS | Parser included; application importer not built yet |
| Preprod SSH access | PARTIAL | SSH alias `top-halal-preprod` configured locally; still need database credentials/scope |
| Server audit | DONE | Audit saved to `docs/generated/server-audit.txt`; PHP 8.4 is functional via `/opt/alt/php84/usr/bin/php`; default PATH PHP remains 8.1.34 |
| Git repository | DONE | Local Git initialized with `origin` set to `git@github-tophalal-codex:pamayo91/top-halal-v2.git`; `main` and `develop` pushed to GitHub |
| Preprod GitHub deploy key | DONE | Dedicated deploy key works with `git@github-tophalal:pamayo91/top-halal-v2.git`; server-side read access tested |
| Preprod deployment strategy | DECIDED | Deploy via SSH and Git pull from `develop`; app checkout must be outside DocumentRoot and Apache must expose only Laravel `public/` |
| Preprod paths | DECIDED | Application path confirmed: `/home/meyo5199/top-halal-v2`; future DocumentRoot confirmed: `/home/meyo5199/top-halal-v2/public`; DocumentRoot not changed yet |
| Preprod clone | DONE | `develop` cloned to `/home/meyo5199/top-halal-v2`; Composer dependencies installed with explicit PHP 8.4; no `.env` created and DocumentRoot unchanged |
| Laravel bootstrap | DONE | Official Laravel 13 project generated by Composer on preprod with PHP 8.4; neutral homepage, `/health`, PHPUnit tests and Playwright scaffold added |
| App DB connection | DONE | Preprod `.env` configured server-side only; `meyo5199_top_halal_v2` read/write verified with the dedicated app user |
| Legacy DB connection | DONE | Existing `meyo5199_th` database uses dedicated SELECT-only user and WordPress prefix `tp_`; SELECT works and INSERT/UPDATE/DELETE/ALTER/DROP are denied |
| Migration: restaurants | DONE (pilot) | Ten deterministic listings migrated and idempotence verified. Opening-hours audit found no usable legacy schedule; no full import authorized. |
| Migration: taxonomies/geography | RETIRED | Historical pilot Geography terms were removed after audit; active taxonomy relations are categories and features only. |
| Migration: articles/pages | DONE (pilot) | Five posts and five pages migrated idempotently; PHP and Playwright desktop/mobile validations are green. |
| Migration: comments | DONE (pilot) | Aggregate audit completed; ten deterministic approved/pending editorial comments migrated idempotently to migrated articles/pages only. Spam, pingbacks, reviews and all non-pilot comments remain untouched. |
| Migration: reviews | DONE (pilot) | Eight deterministic ListingPro reviews migrated idempotently; three malformed/unrated links reported and skipped. |
| Migration: media | DONE (full migration) | Final idempotence run `ab6713de-164a-4216-b320-4cb143e1b184` created no record. 412 former missing inline sources were recovered from the local `wp-contenu` legacy tree; only three 404 sources remain explicit anomalies. No V2 HTML references legacy uploads. The 2026-08-31 V2-only relationship repair restored 892 `restaurant_media.media_asset_id` links with no conflict; O Sha and the repair idempotence pass are validated on preproduction. Public delivery URLs are checksum-versioned to prevent the host cache from mixing distinct media; the remaining editorial HTML URLs are normalized by a V2-only idempotent command. |
| Redirect manager | DONE | 454 application rules active after import; 3 infrastructure rules remain Apache-owned, one normalized duplicate and two manual-review rules are reported. Preproduction exact/regex/query browser checks and PHP suite are green. |
| Public design system | DONE | Blade SSR mobile-first, design tokens locaux, navigation et gabarits publics validés sur préproduction ; les cadres d’images des listes blog, de l’accueil et de l’annuaire restaurant sont désormais uniformes et recadrés |
| Restaurant frontend | DONE | Accueil, blog, collections, fiches, galerie, avis/commentaires et sortie opaque sont validés. Les fiches n’affichent que JPEG/PNG/WebP ; la passe V2 du 2026-09-03 a retiré 5 relations vidéo et 5 assets/fichiers orphelins sur 2 fiches, promu les images restantes sans laisser de fiche sans photo, puis confirmé l’idempotence. |
| Référentiel spécialités | DONE | `Burger`, `Brunch` et `Grillades` sont ajoutés comme catégories V2 versionnées ; ils apparaissent dans les filtres, l’ajout public et l’administration, et sont suggérés par la recherche avant toute association à une fiche publiée. |
| Illustrations de spécialités | DONE | 24 illustrations WebP 1200×800, au nom normalisé, sont liées aux spécialités et 7 380 miniatures-only sont affectées sans créer de couverture. La source Fast-food a été remplacée et ses 6 700 miniatures concernées mises à jour de manière idempotente. Après contrôle de 0 relation restante, la spécialité Mauricienne a été retirée sur demande du propriétaire ; la gate complète est verte. |
| Search/geolocation | DONE | Recherche V2 SSR à double champ partagée accueil/annuaire : Paris par défaut, villes publiées depuis `city_name`, suggestions spécialités/restaurants, priorité locale et géolocalisation uniquement sur action volontaire ; combinaisons de recherche conservées en `noindex,follow`. |
| Accounts/claims | DONE (pilot) | Auth Laravel, changement obligatoire des comptes legacy, claims et modération technique validés ; aucune migration massive utilisateur autorisée. |
| Admin/moderation | DONE | Filament 5 sur `/admin`, modules opérationnels, audit, réglages et modération validés par PHP et Playwright desktop/mobile sur préproduction. La section Utilisateurs permet désormais à un administrateur actif de créer un compte et de lui attribuer le rôle Administrateur, avec mot de passe initial confirmé et changement obligatoire activé par défaut ; aucun secret n’est audité. Le conteneur est fluide sur écran étroit ; la liste des restaurants affiche la date de publication/ajout et ouvre les fiches les plus récentes. |
| Qualité back-office | IN PROGRESS | Normalisation déterministe de texte plain-text, actions « Voir sur le site » et amélioration ciblée des recherches Filament en cours de validation. Les administrateurs actifs disposent d’un raccourci SSR « Éditer » vers la fiche restaurant, l’article ou la page publique consultée, sans asset ni requête supplémentaire pour les visiteurs. |
| Intégrité données migrées | DONE | Audit legacy/V2 des dates, reprise idempotente de `published_at` et des dates média, 32 payloads géographiques malveillants non associés supprimés de V2 ; rapport dans `docs/generated/data-integrity-audit.md`. |
| Audit adresses / GPS | DONE (read-only) | Les 7 704 restaurants V2 et la source WordPress ont été audités sans modification métier ni géocodage. Le blocage principal est l’absence totale de remplissage des colonnes V2 `city_name`/`postal_code` : 4 234 paires CP/ville sont seulement extractibles de l’adresse brute. Rapport et échantillon dans `docs/generated/address-gps-audit.md` et `address-gps-sample.csv`. |
| Pilote Géoplateforme | DONE (sans écriture métier) | 100 restaurants représentatifs interrogés via l’API publique BAN, avec cache, timeout et rate limit. Les résultats sont dans `docs/generated/geocoding-pilot.md`; aucune donnée restaurant, GPS ou Geography n’a été modifiée. |
| Enrichissement adresses | HISTORIQUE | Les rapports d’enrichissement précédents restent archivés, mais leurs métadonnées de qualification/provenance ont été supprimées du modèle. Les adresses legacy et GPS existants restent préservés. |
| Qualification GPS / proximité | REMPLACÉ | `proximity_status` et la couche de confiance/précision ont été supprimés. « Autour de moi » utilise désormais exclusivement latitude/longitude non nulles et dans les plages valides. |
| Consolidation adresses — correctif de persistance | DONE | Les champs administratifs et `address_line1` manquants sont maintenant persistés sans modifier l’adresse historique ni les GPS. Le compteur Phase 5 de 5 022 était un compteur de réponses worker, pas un total BDD ; contrôle dans `docs/generated/address-consolidation-report.md`. |
| Résolution des exceptions adresses | DONE | Phase 5.1 : 34 des 65 exceptions ont été structurées de façon sûre ; 31 restent explicitement classées (3 inutilisables, 3 ambiguës, 5 hors France, 20 suspectes/tests) dans `docs/generated/address-exceptions-report.md`. |
| Composant adresse intelligent | DONE (Phase 6B) | Sélection Géoplateforme obligatoire et résolue côté serveur pour les créations/remplacements, composant partagé public/propriétaire et Filament, code INSEE masqué du formulaire public, déplacement de marqueur GPS-only sans reverse geocoding ni réécriture de l’adresse. Le modèle ne conserve que l’adresse structurée et les coordonnées utiles ; `address` legacy reste intacte. |
| Simplification définitive de localisation | DONE | La migration `2026_09_02_001100` a retiré les 12 colonnes de qualification/géocodage de migration et leurs index, sans modifier `address` ni les données structurées/GPS existantes. Vérification préproduction : schéma conforme, 7 620 restaurants inchangés ; 39 tests PHP ciblés / 247 assertions et la gate complète (84 sentinelles desktop/mobile) passent. |
| Gate non-régression préproduction | DONE | La baseline stricte conserve `restaurants >= 7 618`, `media_assets >= 1 459`, `restaurant_media >= 880` et, après la suppression définitive auditée de 376 comptes explicitement confirmée le 2026-09-06, `users >= 169`. Les 87 suppressions définitives antérieures expliquent le seuil restaurant ; les deux propositions Playwright `pending` portent le total technique à 7 620. La gate complète (80 Playwright desktop/mobile) est verte après ces mises à jour justifiées. |
| Ajout public de restaurant | DONE | Parcours SSR en cinq étapes : adresse Géoplateforme, doublons informatifs, horaires, photos V2 d’au moins 800 px de large pour la couverture comme la galerie, statut `pending`, URLs sortantes privées et e-mail unique conservé à la fois comme contact de la fiche et contexte déposant. Préproduction validée par tests PHP, Playwright desktop/mobile et contrôle navigateur réel. Déploiement initial : `090c024` (2026-08-31). |
| Préproduction — reprise `address_line1` | DONE | 812 fiches strictement concordantes, puis 180 fiches validées manuellement avec suffixe historique visible, ont été corrigées uniquement sur `address_line1` (992 au total). Quatre lignes sans suffixe CP/ville explicite restent intactes. Tous les dry-runs, écritures et seconds passages (0 correction) ont protégé adresse brute, CP/ville/code INSEE, GPS et statuts. Rapports persistants : `docs/generated/address-line1-*.json` sur préproduction. |
| Préproduction — complétion Géoplateforme des adresses absentes | DONE | Sur 1 160 fiches sans les trois champs structurés, Géoplateforme a fourni 1 087 réponses complètes de précision voie/numéro : seule la structure administrative et la provenance ont été ajoutées. Les 73 restantes (62 imprécises, 5 absentes, 6 erreurs) restent intactes. Adresse brute, GPS et statuts ont été préservés ; second passage à 0 écriture. Rapports : `docs/generated/missing-structured-addresses-*.json`. |
| Cohérence validation GPS manuelle | REMPLACÉ | Les statuts `MANUAL`/`ELIGIBLE`/`REVIEW_REQUIRED` ont été supprimés avec la qualification historique. Un déplacement de marqueur modifie uniquement latitude/longitude. |
| Auto-géocodage GPS manquants | DONE (Phase 6A.2) | Après suppression manuelle des dernières fiches non géocodables, les 7 683 restaurants actifs possèdent tous un GPS. La repasse idempotente n’a ajouté ni modifié aucune coordonnée. Rapport : `docs/generated/missing-gps-autogeocoding-report.md`. |
| Corbeille restaurants | DONE | La suppression admin est un soft delete réversible, avec restauration, suppression définitive et vidage audité de la Corbeille. Les anciennes fiches archivées ont été déplacées dans cette Corbeille. |
| Enrichissement web restaurants | DONE (reprise contrôlée) | Checkpoints persistants, réservation de batch, preuves web privées, rapports CSV et garde-fous fermeture. Le batch réservé des IDs 172–271 est clôturé et consolidé dans `docs/generated/web-enrichment/batch-20260831-075919-consolidated.csv`; les alertes fermeture/conflit restent sans modification automatique. |
| Sidebar éditoriale Articles / Pages | DONE | Moteur Blade SSR commun, configurations globales distinctes, overrides sparse, Filament et validation préproduction desktop/mobile ; Articles ON par défaut, Pages OFF par défaut. |
| Advertising | TODO | Native/sponsored placements |
| Email | DONE (transactional preprod) | Laravel queued notifications, signed verification/reset, claims and technical test command validated; no real-recipient campaign. |
| AI provider abstraction | TODO | OpenAI + alternative providers |
| Automated news workflow | TODO | Disclosure configurable |
| SEO structural / routes | DONE | Canonical routes, slash policy, sitemap, environment-aware robots, 404/410, breadcrumbs, JSON-LD and facet policy validated on preproduction. Les fiches restaurant ont des directives meta robots paramétrables (noindex/none exclues du sitemap) et un titre par défaut nom/ville/première spécialité, surchargeable par `seo_title`. Nine approved technical/duplicate non-restaurant URLs are now redirected and excluded; `/blog` and `/mon-compte` are retained. Final visual design remains out of scope. |
| Géographie publique | DONE | Référentiel local `city_code → département → région`, URLs villes précises par code département pour homonymes, désambiguïsations noindex, pages région/département seuil 5 et breadcrumbs visibles/JSON-LD validés. Les collisions ville/département gardent la ville et suffixent automatiquement le département ; Paris et territoires équivalents sont mutualisés. |
| Playwright E2E | DONE | HTTPS preprod smoke passes on desktop/mobile for homepage, `/health`, 404, noindex, robots, cookies and sensitive paths |
| Non-régression permanente | DONE | 14 sentinelles V2 réelles enregistrées, dont une prévisualisation de proposition `pending` ; 17 tests PHP de conservation/intégrité/back-office et 108 tests Playwright desktop/mobile passent sur préproduction, avec contrôle final des médias, relations, compteurs, 500, console/réseau et logs Laravel. |
| Public exposure checks | DONE | `/`, `/health`, 404 and sensitive paths checked; `.env`, `composer.json`, `artisan`, `storage/`, `vendor/`, `.git/` are not publicly readable |
| Preproduction indexing protection | DONE | `X-Robots-Tag: noindex, nofollow` is active and `robots.txt` disallows all crawling |
| Legacy inventory | DONE | `legacy:inventory` ran against `meyo5199_th` via SELECT-only `legacy_wp`; reports are in `docs/generated/` |
| Migration mapping | IN PROGRESS | Restaurant pilot schema and selected IDs documented; other content mappings remain draft |
| PageSpeed/CWV gates | TODO | Representative templates |
| Production cutover | TODO | Full migration + delta + URL/redirect validation |
