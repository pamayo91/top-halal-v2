<x-layouts.app title="Le guide | Top Halal" canonical="{{ route('blog.index') }}" robots="{{ request('page') || $category ? 'noindex,follow' : 'index,follow' }}"><x-slot:head><style>.article-grid-wide{grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem}.article-grid-wide .card-image{height:clamp(130px,13vw,160px);aspect-ratio:auto!important;min-height:0;overflow:hidden;flex:none}.article-grid-wide .card-image img{width:100%!important;height:100%!important;object-fit:cover!important}.article-grid-wide .article-card{display:flex;flex-direction:column;padding:1rem}.article-grid-wide .article-card h2{min-height:2.65em;font-size:1.15rem}@media(min-width:761px){.article-grid-wide{grid-template-columns:repeat(4,minmax(0,1fr))}}</style></x-slot:head>
<section class="page-header"><div class="shell"><nav class="breadcrumbs" aria-label="Fil d’Ariane"><a href="{{ route('home') }}">Accueil</a><span aria-hidden="true">/</span><span aria-current="page">Le guide</span></nav><h1>Le guide Top Halal</h1><p>Conseils, découvertes et informations pratiques.</p></div></section>
<section class="section shell">@if($categories->isNotEmpty())<nav class="tags" aria-label="Catégories du guide"><a href="{{ route('blog.index') }}">Toutes les catégories</a>@foreach($categories as $item)<a href="{{ $item->slug === 'muslim-gourmet' ? route('blog.muslim-gourmet') : route('blog.index', ['categorie' => $item->slug]) }}">{{ $item->name }}</a>@endforeach</nav>@endif<div class="article-grid article-grid-wide">@forelse($articles as $article)<x-article-card :article="$article" />@empty <div class="empty-state"><h2>Aucun article dans cette catégorie.</h2></div>@endforelse</div>{{ $articles->links() }}</section>
</x-layouts.app>
