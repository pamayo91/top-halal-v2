<x-layouts.app title="Le guide | Top Halal" canonical="{{ route('blog.index') }}" robots="{{ request('page') ? 'noindex,follow' : 'index,follow' }}"><section class="page-header"><div class="shell"><nav class="breadcrumbs" aria-label="Fil d’Ariane"><a href="{{ route('home') }}">Accueil</a><span aria-hidden="true">/</span><span aria-current="page">Le guide</span></nav><h1>Le guide Top Halal</h1><p>Conseils, découvertes et informations pratiques.</p></div></section><section class="section shell"><div class="article-grid article-grid-wide">@forelse($articles as $article)<article class="article-card"><p class="eyebrow">Article</p><h2><a href="{{ route('editorial.show', $article->slug) }}">{{ $article->title }}</a></h2>@if($article->seo_description)<p>{{ $article->seo_description }}</p>@endif @if($article->legacy_published_at)<p class="muted">{{ $article->legacy_published_at->translatedFormat('j F Y') }}</p>@endif</article>@empty <div class="empty-state"><h2>Le guide arrive bientôt.</h2></div>@endforelse</div>{{ $articles->links() }}</section></x-layouts.app>
