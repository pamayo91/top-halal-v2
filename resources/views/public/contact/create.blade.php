<x-layouts.app :title="'Contact - Top Halal'"><section class="shell section"><p class="eyebrow">Contact</p><h1>Nous contacter</h1><p>{{ $settings['introduction'] ?? 'Une question ou une suggestion ? Écrivez-nous.' }}</p><form method="post" action="{{ route('contact.store') }}" class="form-card" data-disable-on-submit>@csrf<div class="hp" aria-hidden="true"><label for="website">Ne pas remplir</label><input id="website" name="website" tabindex="-1" autocomplete="off"></div><label>Nom<input name="name" value="{{ old('name') }}" required maxlength="120" autocomplete="name"></label>@error('name')<p class="field-error">{{ $message }}</p>@enderror<label>E-mail<input type="email" name="email" value="{{ old('email') }}" required maxlength="254" autocomplete="email"></label>@error('email')<p class="field-error">{{ $message }}</p>@enderror<label>Sujet<input name="subject" value="{{ old('subject') }}" required maxlength="180"></label>@error('subject')<p class="field-error">{{ $message }}</p>@enderror<label>Message<textarea name="message" required maxlength="5000" rows="8">{{ old('message') }}</textarea></label>@error('message')<p class="field-error">{{ $message }}</p>@enderror<button class="button" type="submit">Envoyer le message</button></form></section></x-layouts.app>
