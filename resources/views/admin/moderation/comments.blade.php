@extends('admin.layout') @section('content')<h1>Modération des commentaires</h1><form><input name="q" value="{{ request('q') }}" placeholder="Auteur ou contenu"><select name="status"><option value="">Tous</option>@foreach(['pending','approved','rejected','spam'] as $s)<option @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select><button>Filtrer</button></form><table><tr><th>Contenu</th><th>Auteur</th><th>Message</th><th>Parent</th><th>Statut</th><th>Action</th></tr>@foreach($items as $item)<tr><td>{{ $item->article?->title ?? $item->page?->title }}</td><td>{{ $item->author_name }}</td><td>{{ $item->content }}</td><td>{{ $item->parent_id }}</td><td>{{ $item->status }}</td><td><form method="post" action="{{ route('admin.comments.status',$item) }}">@csrf @method('PATCH')<select name="status">@foreach(['approved','rejected','spam'] as $s)<option>{{ $s }}</option>@endforeach</select><button>Appliquer</button></form></td></tr>@endforeach</table>{{ $items->links() }}@endsection
