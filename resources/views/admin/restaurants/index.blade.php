@extends('admin.layout') @section('content')<div class="actions"><h1>Restaurants</h1><a href="{{ route('admin.restaurants.create') }}">Créer</a></div><form><input name="q" value="{{ request('q') }}" placeholder="Nom ou ville"><select name="status"><option value="">Tous statuts</option>@foreach(['draft','pending','published','reported','archived'] as $s)<option @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select><button>Filtrer</button></form><table><tr><th>Nom</th><th>Ville</th><th>Statut</th><th>Catégories</th><th></th></tr>@foreach($restaurants as $r)<tr><td>{{ $r->name }}</td><td>{{ $r->city_name }}</td><td>{{ $r->status }}</td><td>{{ $r->categories->pluck('name')->join(', ') }}</td><td><a href="{{ route('admin.restaurants.edit',$r) }}">Éditer</a></td></tr>@endforeach</table>{{ $restaurants->links() }}@endsection
