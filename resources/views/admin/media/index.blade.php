@extends('admin.layout') @section('content')<h1>Médiathèque</h1><form method="post" enctype="multipart/form-data" action="{{ route('admin.media.store') }}">@csrf<label>Image<input type="file" name="file" accept="image/jpeg,image/png,image/webp" required></label><label>Texte alternatif<input name="alt_text"></label><label>Légende<input name="caption"></label><button>Importer</button></form><table><tr><th>Aperçu</th><th>Type/dimensions/poids</th><th>Alt / légende</th><th>Variantes</th><th></th></tr>@foreach($assets as $asset)<tr><td>@if(str_starts_with($asset->mime,'image/'))<img src="{{ route('media.show',$asset) }}" alt="" width="100">@endif</td><td>{{ $asset->mime }}<br>{{ $asset->width }}×{{ $asset->height }} · {{ $asset->bytes }} o</td><td><form method="post" action="{{ route('admin.media.update',$asset) }}">@csrf @method('PUT')<input name="alt_text" value="{{ $asset->alt_text }}"><input name="caption" value="{{ $asset->caption }}"><button>Enregistrer</button></form></td><td>{{ $asset->variants_count }}</td><td>{{ $asset->created_at }}</td></tr>@endforeach</table>{{ $assets->links() }}@endsection
