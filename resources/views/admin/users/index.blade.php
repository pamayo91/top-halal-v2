@extends('admin.layout') @section('content')<h1>Utilisateurs</h1><form><input name="q" value="{{ request('q') }}" placeholder="Nom ou e-mail"><select name="role"><option value="">Tous rôles</option>@foreach(['user','restaurant_owner','admin'] as $role)<option @selected(request('role')===$role)>{{ $role }}</option>@endforeach</select><button>Filtrer</button></form><table><tr><th>Nom</th><th>E-mail</th><th>Rôle</th><th>Statut</th><th>Restaurants</th><th>Créé</th><th>Actions</th></tr>@foreach($users as $user)<tr><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td colspan="4"><form class="actions" method="post" action="{{ route('admin.users.update',$user) }}">@csrf @method('PATCH')<select name="role">@foreach(['user','restaurant_owner','admin'] as $role)<option @selected($user->role===$role)>{{ $role }}</option>@endforeach</select><select name="status">@foreach(['active','disabled'] as $status)<option @selected($user->status===$status)>{{ $status }}</option>@endforeach</select><label><input type="checkbox" name="must_change_password" value="1" @checked($user->must_change_password)> Changement requis</label><button>Enregistrer</button></form></td><td><form method="post" action="{{ route('admin.users.reset',$user) }}">@csrf<button>Reset password</button></form></td></tr>@endforeach</table>{{ $users->links() }}@endsection
