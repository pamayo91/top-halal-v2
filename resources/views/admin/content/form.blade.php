@extends('admin.layout') @section('content')<h1>{{ $item->exists?'Modifier':'Créer' }} {{ rtrim($type,'s') }}</h1><form method="post" action="{{ $item->exists?route('admin.content.update',[$type,$item]):route('admin.content.store',$type) }}">@csrf @if($item->exists)@method('PUT')@endif<div class="grid"><label>Titre<input name="title" required value="{{ old('title',$item->title) }}"></label><label>Slug<input name="slug" value="{{ old('slug',$item->slug) }}"></label><label>Statut<select name="status">@foreach(['draft','published'] as $s)<option @selected(old('status',$item->status)===$s)>{{ $s }}</option>@endforeach</select></label><label>SEO title<input name="seo_title" value="{{ old('seo_title',$item->seo_title) }}"></label><label>SEO description<input name="seo_description" value="{{ old('seo_description',$item->seo_description) }}"></label><label>Robots<input name="seo_robots" value="{{ old('seo_robots',$item->seo_robots) }}"></label></div><label>Extrait<textarea name="excerpt">{{ old('excerpt',$item->excerpt) }}</textarea></label><label>Contenu HTML (sanitisé lors de l’enregistrement)<textarea name="content_html" style="min-height:24rem">{{ old('content_html',$item->content_html) }}</textarea></label><button>Enregistrer</button></form>@endsection
