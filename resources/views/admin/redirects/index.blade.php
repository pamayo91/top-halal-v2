@extends('admin.layout') @section('content')<div class="actions"><h1>Redirections</h1><a href="{{ route('admin.redirects.create') }}">Créer</a></div><form><input name="q" value="{{ request('q') }}" placeholder="Source ou destination"><button>Rechercher</button></form><form method="post" action="{{ route('admin.redirects.test') }}">@csrf<input name="url" required placeholder="/ancienne-url?x=1"><button>Tester</button>@if(session('test'))<pre>{{ json_encode(session('test'),JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre>@endif</form><table><tr><th>Source</th><th>Destination</th><th>Type</th><th>Code</th><th>État</th><th>Hits</th><th></th></tr>@foreach($rules as $rule)<tr><td>{{ $rule->source_path }}</td><td>{{ $rule->destination }}</td><td>{{ $rule->match_type }}</td><td>{{ $rule->status_code }}</td><td>{{ $rule->is_active?'active':'inactive' }}</td><td>{{ $rule->hit_count }}</td><td><a href="{{ route('admin.redirects.edit',$rule) }}">Éditer</a></td></tr>@endforeach</table>{{ $rules->links() }}@endsection
