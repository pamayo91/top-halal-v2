@extends('admin.layout') @section('content')<h1>Tableau de bord</h1><div class="grid">@foreach($counts as $label=>$count)<div class="card"><strong>{{ str_replace('_',' ',ucfirst($label)) }}</strong><br>{{ $count }}</div>@endforeach</div><h2>Actions récentes</h2><table><tr><th>Date</th><th>Admin</th><th>Action</th><th>Objet</th></tr>@forelse($audits as $audit)<tr><td>{{ $audit->created_at }}</td><td>{{ $audit->admin?->name ?? 'Supprimé' }}</td><td>{{ $audit->action }}</td><td>{{ class_basename($audit->subject_type) }} #{{ $audit->subject_id }}</td></tr>@empty<tr><td colspan="4">Aucune action.</td></tr>@endforelse</table>@endsection
