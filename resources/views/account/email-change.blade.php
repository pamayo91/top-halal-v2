<x-layouts.app title="Modifier mon adresse e-mail | Top Halal" :hide-flash="true">
    <section class="contact-page auth-page"><div class="shell auth-shell"><div class="contact-form-wrap auth-form-wrap"><aside class="contact-sticker"><span>Mon profil et sécurité</span></aside><div class="contact-form-card auth-form-card"><header><p class="eyebrow">Adresse e-mail</p><h1>Modifier mon adresse e-mail</h1><p>Votre adresse actuelle reste active jusqu’à la confirmation du lien envoyé à la nouvelle adresse.</p></header>@if($errors->any())<div class="contact-errors" role="alert"><p>Certains champs demandent votre attention.</p></div>@endif<form class="contact-form" method="post" action="{{ route('account.email-change.store') }}">@csrf<div class="contact-field"><label for="email">Nouvelle adresse e-mail</label><div class="contact-input"><input id="email" name="email" type="email" value="{{ old('email') }}" autocomplete="email" required></div>@error('email')<p class="field-error">{{ $message }}</p>@enderror</div>@if(filled(auth()->user()->password))<div class="contact-field"><label for="current_password">Mot de passe actuel</label><div class="contact-input"><input id="current_password" name="current_password" type="password" autocomplete="current-password" required></div>@error('current_password')<p class="field-error">{{ $message }}</p>@enderror</div>@endif<button class="button contact-submit" type="submit">Envoyer le lien de confirmation <span aria-hidden="true">→</span></button></form></div></div></div></section>
</x-layouts.app>
