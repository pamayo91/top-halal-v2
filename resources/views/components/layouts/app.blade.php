<!doctype html>
<html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#0b5d4b"><meta name="robots" content="{{ $robots ?? 'noindex,follow' }}"><title>{{ $title ?? 'Top Halal' }}</title>@isset($description)<meta name="description" content="{{ $description }}">@endisset @isset($canonical)<link rel="canonical" href="{{ $canonical }}">@endisset @if(! app()->environment('testing')) @vite(['resources/css/app.css', 'resources/js/app.js']) @endif {{ $head ?? '' }}</head>
<body><a class="skip-link" href="#contenu">Aller au contenu</a><header class="site-header"><div class="shell nav-wrap"><a class="brand" href="{{ route('home') }}" aria-label="Top Halal, accueil">Top<span>Halal</span></a><nav class="main-nav" aria-label="Navigation principale"><a href="{{ route('restaurants.index') }}">Restaurants</a><a href="{{ route('blog.index') }}">Le guide</a><a href="{{ route('restaurants.index') }}#recherche">Rechercher</a></nav><div class="nav-account">@auth <a href="{{ route('account.dashboard') }}">Mon compte</a><form method="post" action="{{ route('logout') }}">@csrf<button class="link-button" type="submit">Déconnexion</button></form>@else <a href="{{ route('login') }}">Connexion</a><a class="button button-small" href="{{ route('register') }}">S’inscrire</a>@endauth</div><button class="menu-toggle" type="button" aria-expanded="false" aria-controls="mobile-nav">Menu</button></div><nav id="mobile-nav" class="mobile-nav shell" aria-label="Navigation mobile" hidden><a href="{{ route('restaurants.index') }}">Restaurants</a><a href="{{ route('blog.index') }}">Le guide</a>@auth<a href="{{ route('account.dashboard') }}">Mon compte</a>@else<a href="{{ route('login') }}">Connexion</a>@endauth</nav></header><main id="contenu">@if(session('status'))<div class="shell flash" role="status">{{ session('status') }}</div>@endif @if($errors->any())<div class="shell flash flash-error" role="alert"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif {{ $slot }}</main><footer class="site-footer"><div class="shell footer-grid"><a class="brand" href="{{ route('home') }}">Top<span>Halal</span></a><p>Le guide indépendant pour trouver un restaurant halal.</p><nav aria-label="Liens de pied de page"><a href="{{ route('restaurants.index') }}">Restaurants</a><a href="{{ route('blog.index') }}">Le guide</a></nav></div></footer></body></html>
