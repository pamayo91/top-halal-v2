@props(['article'])
@php($asset = $article->featuredMedia?->asset)
<article class="article-card">@if($asset)<a class="card-image" href="{{ route('editorial.show', $article->slug) }}"><img src="{{ route('media.show', $asset) }}" width="{{ $asset->width }}" height="{{ $asset->height }}" loading="lazy" alt="{{ $asset->alt_text ?: $article->title }}"></a>@endif<p class="eyebrow">{{ $article->categories->pluck('name')->join(' · ') ?: 'Article' }}</p><h2><a href="{{ route('editorial.show', $article->slug) }}">{{ $article->title }}</a></h2>@if($article->seo_description)<p>{{ $article->seo_description }}</p>@endif @if($article->legacy_published_at)<p class="muted">{{ $article->legacy_published_at->translatedFormat('j F Y') }}</p>@endif</article>
