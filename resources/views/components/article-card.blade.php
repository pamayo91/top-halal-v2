@props(['article'])
@php($asset = $article->featuredMedia?->asset ?: $article->contentMedia->first(fn ($media) => $media->role === 'inline' && $media->asset)?->asset)
<article class="article-card"><a class="card-image" href="{{ route('editorial.show', $article->slug) }}" aria-label="Lire {{ $article->title }}">@if($asset)<img src="{{ route('media.show', $asset) }}" width="{{ $asset->width }}" height="{{ $asset->height }}" loading="lazy" alt="{{ $asset->alt_text ?: $article->title }}">@else<span class="image-placeholder" aria-hidden="true">📝</span>@endif</a><p class="eyebrow">{{ $article->categories->pluck('name')->join(' · ') ?: 'Article' }}</p><h2><a href="{{ route('editorial.show', $article->slug) }}">{{ $article->title }}</a></h2>@if($article->seo_description)<p>{{ $article->seo_description }}</p>@endif @if($article->legacy_published_at)<p class="muted">{{ $article->legacy_published_at->translatedFormat('j F Y') }}</p>@endif</article>
